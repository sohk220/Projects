define service{
        use                             generic-omneon-service         ; Name of service template to use
	hostgroup_name			omneon-ldap-servers
        service_description             LDAP
        normal_check_interval           1
        contact_groups                  omneon-admins
	notification_interval		120
        notification_options            w,c,r
        check_command                   check_ldap!dc=nydre,dc=cnn
}
