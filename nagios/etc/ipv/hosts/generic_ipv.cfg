define host{
        name                            generic-ipv-host ; The 'name' of this service template, referenced in other service definitions
        active_checks_enabled           1       ; Active service checks are enabled
        passive_checks_enabled          1       ; Passive service checks are enabled/accepted
        check_freshness                 0       ; Default is to NOT check service 'freshness'
        event_handler_enabled           1       ; Host event handler is enabled
        flap_detection_enabled          1       ; Flap detection is enabled
        process_perf_data               1       ; Process performance data
        retain_status_information       1       ; Retain status information across program restarts
        retain_nonstatus_information    1       ; Retain non-status information across program restarts
        register                        0       ; DONT REGISTER THIS DEFINITION - ITS NOT A REAL SERVICE, JUST A TEMPLATE!
        check_period                    24x7    ; Check 24 Hours a day 7 Days a Week
        max_check_attempts              10      ; Maximum attempts to check service_description
        normal_check_interval           5       ; Norm check interval
	notifications_enabled           1       ; Host notifications are enabled
	notification_interval           30
        notification_period             24x7
        notification_options            d,u,r
        register                        0       ; DONT REGISTER THIS DEFINITION - ITS NOT A REAL HOST, JUST A TEMPLATE!
}
